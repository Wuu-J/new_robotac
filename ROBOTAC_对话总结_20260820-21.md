# ROBOTAC 项目对话总结（2026-08-20 ~ 08-21）

> **用途**：本文件是 WorkBuddy 与用户两天对话的完整总结，供 Ubuntu 系统中的 WorkBuddy 读取以恢复项目上下文。
> **使用方法**：在 Ubuntu 中打开 WorkBuddy，把本文件所在目录（`~/matrix_robotac_worksapce/`）作为工作区，或直接告诉 WorkBuddy「先读 ROBOTAC_对话总结_20260820-21.md」。
> **配套文档**：《注意事项.md》（红线文档，开发前必读）、《Ubuntu装好后完整步骤_准备与代码传入.md》（实操指南）、《双系统安装指南_E盘2TB专用版.md》

---

## 0. 项目一句话背景

第二十五届全国大学生机器人大赛 ROBOTAC 四足机甲挑战赛（MATRiX 仿真平台 = MuJoCo + UE5 + CARLA，Ubuntu 22.04 + ROS2 Humble）。机器人固定用「钢镚 L1（xgb）」。中期检查 9 月 12 日前提交（PCD 点云 + 视频），决赛 10 月中旬天津。

---

## 1. 时间线大事记

### 2026-08-20（第一天）

| 时间 | 事件 |
|------|------|
| 上午 | 用户在 VMware 虚拟机跑 `./run_sim.sh xgb 1 0 0 0` 报错：**Vulkan device could not be created** |
| 诊断 | 根因：`UeSim/Config/DefaultEngine.ini` 写死 `TargetedRHIs=SF_VULKAN_SM6`（要求 Vulkan 1.3 + SM6），VMware 虚拟显卡（SVGA）不满足。**结论：VMware 里跑不了 UE 仿真** |
| 方案讨论 | 对比 VMware / 双系统 / 云 GPU 三种方案 → 用户选择云 GPU（AutoDL） |
| 下午 | 开通 **AutoDL 实例**：RTX 3080 Ti 12GB，Ubuntu-nvidia 22.04 镜像，¥0.74/小时，防火墙选「非Web服务器推荐」 |
| 部署 | SSH 部署：装 ROS2 Humble（踩坑：AutoDL 镜像缺 `/etc/apt/sources.list.d/` 目录，需先 mkdir）、上传 7.5GB 项目代码（4 线程并行 SFTP，~19Mbps） |
| 启动 | `./run_sim.sh xgb 1 1 0 0`（离屏渲染）成功，验证传感器：/front_lidar 10Hz、相机 10Hz、IMU ~500Hz |
| 傍晚 | 尝试 **PixelStreaming 远程画面**：3 种方案（offscreen+PS / xvfb+PS / 禁 DistanceField+PS）全部失败，卡死在渲染线程 `OriginX <= OriginMax` ensure 死循环 → **判定为 UeSim 打包 bug，放弃 PS** |
| 夜晚 | 改用 **bag 录制回放**方案：录制 30s bag（225MB，296 帧雷达+相机+IMU+odom），下载到本地，在 VMware 虚拟机用 rviz2 回放 |

### 2026-08-21（第二天）

| 时间 | 事件 |
|------|------|
| 上午 | 用户折腾 rviz2 回放显示（点云不显示问题）|
| 排查 | ①By topic 看不到 → 用 `ros2 bag info` 确认 5 个 topic ②Fixed Frame `base_link` 不存在（bag 无 TF）③点云 frame_id 实测 = **`lidar`**，但 TF 树无此坐标 → 需发 static_transform_publisher 或把 Fixed Frame 改 `lidar` |
| 结论 | rviz2 看不到点云的根因：**bag 没录 TF + frame_id 是 `lidar`**。解决：改 Fixed Frame = `lidar`，或手动发布静态 TF |
| 中午 | 用户问 xterminal 连服务器（SSH 密码认证失败过一次，疑密码格式），确认 xterminal 可用于命令行但显示不了 GUI |
| 决策 | 用户放弃折腾云端画面，**决定装双系统**：Windows + Ubuntu 22.04，放在 E 盘 |
| 磁盘实测 | 磁盘0 = Acer SSD N5000 2TB（E 盘，剩 1.8T）；磁盘1 = WD SN740 512G（EFI + C 189G + D 286G）；磁盘2 = 59G USB |
| 方案 | 缩容 E 盘 150GB（153600 MB）给 Ubuntu；WD 盘完全不动；GRUB 装 WD 盘 EFI 分区。用户确认：NVMe 固态 ✅ 数据已备份 ✅ 分 150G ✅ |
| 产出 | 生成《双系统安装指南_E盘2TB专用版.md》 |
| 傍晚 | 用户改用 **Ventoy** 做启动盘（比 Rufus 方便，可反复用）→ 已给出 Ventoy 制作步骤 + Ubuntu 22.04.5 ISO 清华源下载 + BIOS 设置 |

---

## 2. 关键技术结论（踩坑记录）

### 2.1 VMware 跑不了 UE 仿真（已实锤）
- 现象：UeSim 弹 `Vulkan device could not be created at the project's supported feature levels`
- 根因：`DefaultEngine.ini` 的 `TargetedRHIs=SF_VULKAN_SM6` 要求 Vulkan ≥ 1.3 + SM6 级 GPU，VMware SVGA 虚拟显卡达不到
- **不要在 VMware 里跑 UeSim**。仿真必须在有 NVIDIA 独立显卡的物理机跑 Ubuntu，或云 GPU

### 2.2 AutoDL 云服务器部署（成功，供双系统之前过渡用）
- 实例信息：ubuntu@117.50.221.86，端口 22（密码见用户 xterminal 配置）
- 镜像必须选「系统镜像 → Ubuntu-nvidia 22.04」；防火墙选「非Web服务器推荐」
- 部署踩坑速记：
  - AutoDL 镜像删了 `/etc/apt/sources.list.d/` → 先 `mkdir -p` 再写 ros2.list
  - UeSim 拒绝 root → 必须普通用户启动
  - `libfastcdr.so.1` 缺失 → 先 `source /opt/ros/humble/setup.bash`
  - run_sim.sh 有 flock 启动锁 → 重启前先杀旧进程
  - **PixelStreaming 不可用**（UeSim 打包 bug，渲染线程死循环）→ 弃用
- 费用 ¥0.74/时，不用时关机（无卡开机可保留数据）

### 2.3 bag 回放与 rviz2 关键点
- 录制：服务器上 `ros2 bag record -a -o ~/bags/xxx`
- bag 内容（matrix_30s，29.6s）：5 个 topic
  - `/odom/mujoco_odom` (Odometry, 14760)
  - `/imu` (Imu, 14766)
  - `/front_lidar` (**PointCloud2**, 296) ← 点云 frame_id = **`lidar`**
  - `/front_depth/image/compressed` (CompressedImage, 296)
  - `/front_camera/image/compressed` (CompressedImage, 296)
- rviz2 配置：
  - 回放命令加 `-l`（循环），否则 30s 播完就没了
  - **bag 没录 TF** → Fixed Frame 改成 `lidar`（或发 static_transform_publisher）
  - 点云显示不出来时：滚轮放大 / Distance 调小 / Style 改 Points

### 2.4 双系统方案（当前执行中）
- 磁盘布局：Windows 在 WD 512G（C/D/EFI），Ubuntu 装 Acer 2TB 的 150GB 未分配区
- 引导：Ubuntu 的 GRUB 装到 **WD 盘的 EFI 分区**
- 用户当前进度：已下载 Ventoy + Ubuntu ISO 阶段（2026-08-21 傍晚）

---

## 3. 当前状态与下一步

### 状态
- [x] AutoDL 云仿真跑通（备用方案，随时可用）
- [x] bag 录制/回放链路跑通
- [ ] 双系统安装进行中（Ventoy 启动盘制作 → 装 Ubuntu → 装驱动）
- [ ] 本地仿真窗口验证未做（等双系统装好）

### 下一步（按优先级）
1. **完成双系统安装**（按《双系统安装指南_E盘2TB专用版.md》）
2. **代码传入 Ubuntu**（U 盘拷贝或 SCP，见《Ubuntu装好后完整步骤_准备与代码传入.md》）
3. **验证本地仿真**：`./run_sim.sh xgb 1 0 0 0`（有窗口），确认 vulkaninfo ≥ 1.3
4. **SDK 控制测试**：跑 `highlevel_demo_Interactive.py`（数字菜单控制：1 站立 / 2 趴下 / 3-8 移动转向）
5. **中期检查准备**（9/12 前）：SLAM 建图 MVP（订阅 /front_lidar 点云 → 累积 → 导出 PCD，**PCD 禁止后处理**）+ 演示视频

### 待决问题（用户需确认）
- 任务三激光发射器的触发方式（SDK 无射击接口，待研究）
- 任务三识别相机源（机器狗自带相机 RTSP: 192.168.234.1:8554/test）

---

## 4. 关键信息速查表

| 项目 | 值 |
|------|-----|
| 仿真启动命令 | `cd ~/matrix_robotac_worksapce/matrix_robotac_first && ./run_sim.sh xgb 1 0 0 0`（窗口）/ `xgb 1 1 0 0`（离屏）|
| 仿真 SDK 地址 | 127.0.0.1:43988（initRobot("127.0.0.1", 43988, "127.0.0.1")）|
| 真机 WiFi IP | 192.168.234.1（sdk_config.yaml target_ip 需改，43988 端口）|
| 传感器 topic | /front_lidar (PointCloud2) /front_camera/image/compressed /front_depth/image/compressed /imu /odom/mujoco_odom /fire |
| SDK demo 路径 | `deps/zsibot_sdk/demo/zsl-1/python/examples/highlevel_demo_Interactive.py` |
| SDK 头文件 | `deps/zsibot_sdk/include/zsl-1/highlevel.h`（函数名以它为准！standUp/checkConnect/cancelTwoLegStand）|
| move 速度死区 | vx: ±0.05~3 m/s；vy: ±0.1~1 m/s；yaw: ±0.02~3 rad/s；停止传 0 |
| 3 秒规则 | SDK 3 秒无数据 → 机器人自动趴下，控制循环必须 < 3s |
| 建图评分 | 100 分制 = 精度 40（RMSE≤0.05m 满分）+ 完整度 40 + 质量 20（倒扣）|
| 中期检查 | 9 月 12 日前；提交 PCD（禁后处理）+ 视频（禁剪辑，mp4 ≤15min ≤500MB）|

---

## 5. 对 Ubuntu 中 WorkBuddy 的交接说明

接下来在 Ubuntu 中继续工作时，请：

1. **先读**本目录下的 `注意事项.md`（红线文档）—— 每次生成/修改代码前必须读
2. **开发约定**（用户明确要求，红线级）：
   - 函数名以 `highlevel.h` 头文件为准（不要照文档写 standUP/checkConnection）
   - 不确定的地方（API 用法、参数范围、Topic 名、IP/端口、规则、阈值）**先问用户，不要自己假设**
   - move 速度避开死区；状态机顺序：移动中先 move(0,0,0) 停稳再切动作
   - 仿真 initRobot 填 127.0.0.1；PCD 禁止后处理
3. **当前任务焦点**：等用户装好双系统 → 协助验证本地仿真 → 开发 SLAM 建图（中期检查 MVP）

---

> 生成时间：2026-08-21 · 由 WorkBuddy 在 Windows 侧整理，供 Ubuntu 侧恢复上下文
