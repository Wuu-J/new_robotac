# ROBOTAC 四足机甲挑战赛 — 注意事项手册

> **用途**：本手册汇总了赛题 PDF（全 25 页）与项目全部 md 文档中的「容易出问题、需要注意」的地方。
> **红线**：**每次为本项目生成或修改代码前，必须先阅读本手册**；对本项目任何不确定的地方（API 用法、参数范围、Topic 名称、IP/端口、规则、阈值），**不要自己假设生成，先问用户**。

---

## 0. 使用说明与开发约定（红线级）

用户原始指令（2026-08-19）：

> "请仔细阅读本项目所有文件夹下的md文档和pdf，给我整理出容易出现问题的，需要注意的地方并写到一个md文档中。并且之后每一次生成代码都要注意这些问题，以后不确定的地方不要自己生成，先问我。"

**两条硬约束：**

1. **先看本手册再动笔**：每次生成/修改任何代码前，先 Read 本文件，对照其中约束（函数名以 `highlevel.h` 为准、move 速度死区、状态机顺序、3 秒无数据趴下、端口/IP 三处一致、仿真 initRobot 填 127.0.0.1、PCD 禁止后处理等）后再写。
2. **不确定先问**：对 SDK API 用法、参数范围、Topic 名称、IP/端口、比赛规则、评分阈值、设备参数、配置文件含义等任何不确定点，先问用户，等确认后再写代码。

---

## 1. 比赛硬性规则（来自赛题 PDF 全 25 页）

### 1.1 关键时间节点

| 事项 | 时间 | 备注 |
|------|------|------|
| **中期检查（线上）** | **9 月 12 日前** | 淘汰关卡，错过无法进决赛；建议提前 3~5 天提交 |
| **全国总决赛（线下）** | **10 月中旬** | 地点：天津现代职业技术学院 |
| 晋级名额 | 通过中期检查队伍中 **至多 50 支** | 中期检查要争取高分排名，不只是及格 |

### 1.2 参赛要求（易违规点）

- 每队 **≤3 名学生**，**1 名指导教师**
- 同一学校 **≤5 支**队伍
- 每名学生**限报 1 支**队伍
- **不得跨校组队**
- **不得随意更换成员**
- 入围决赛但**未到现场者取消资格**

### 1.3 中期检查要求

- 在 **MATRiX 仿真平台**完成（勿用第三方仿真替代）
- **手动驾驶或自主驾驶皆可**（可先用遥操作跑通拿基础分）
- 提交物：演示视频 + PCD 点云文件

### 1.4 视频硬性要求

| 项目 | 要求 |
|------|------|
| 格式 | **mp4 / mov / avi**（建议统一 mp4/H.264）|
| 时长 | **≤ 15 分钟** |
| 大小 | **≤ 500MB**（建议码率压到 ~4~5 Mbps）|
| 编辑 | **禁止剪辑、拼接、删减**（一镜到底，原始文件直接提交）|

### 1.5 PCD 原生要求（高风险）

- **仅收取原生 PCD 文件（未经任何后处理）**
- **禁止对结果进行人工修改、二次加工**
- 不要手工删噪点、补洞、做格式转换、坐标系二次变换、降采样——直接用算法原始输出
- 决赛现场用 **U 盘**拷贝，文件**按工位号命名**

### 1.6 三大任务分值与时限

| 任务 | 内容 | 分值 | 时限 | 形式 |
|------|------|------|------|------|
| 任务一 | 场地探索与建模（SLAM 建图）| 30 = 建图 25 + 时间 5 | 30 分钟 | 仿真 |
| 任务二 | 路径规划夺分（宝箱）| 30 = 宝箱 20 + 时间 10 | 30 分钟 | 仿真 |
| 任务三 | 视觉识别与自动打击（AprilTag）| 40 = 射击 30 + 时间 10 | 60 分钟 | **实机** |

- 任务二**每成功触发 1 个宝箱，任务三 +1 发弹药**（弹药联动，任务二务必多攒）
- 任务三：**基础弹药 5 发** + 任务二奖励
- 靶标为 **AprilTag，ID 0 / 1 / 2**
- 同一靶可多次射击**取最高分**；共**三次评分机会，保留最高分**
- **任务三禁止人工瞄准或人工控制**，必须全自动；系统提供**脱靶反馈**，据此实时修正

### 1.7 建图评分细则（100 分制）⭐核心决策依据

> 出处：赛题 PDF 第 24 页。建图结果从精度、完整度、质量三个维度综合评分，满分 100 分。

**精度得分（40 分）— RMSE 阈值：**

| RMSE | 得分 | 等级 |
|------|------|------|
| ≤ 0.05 m | 40 | 优秀 |
| ≤ 0.10 m | 35 | 良好 |
| ≤ 0.20 m | 28 | 中等 |
| ≤ 0.40 m | 15 | 偏差较大 |
| > 0.40 m | 0 | 不合格 |

**完整度得分（40 分）：**
- 覆盖率 = 被覆盖点数 / 总采样点数
- 覆盖判定阈值：**0.10 m**
- 得分 = 覆盖率 × 40（覆盖率过低有封顶）
- 评估墙壁与地面覆盖程度

**质量得分（20 分）— 基础 20 分，倒扣制（独立计算可叠加，最低 0 分）：**

| 项目 | >10% | >30% |
|------|------|------|
| 无效点 | -3 | -8 |
| 噪声点 | -3 | -8 |

**封顶与惩罚：**

| 条件 | 封顶 |
|------|------|
| 无效点 > 50% | 60 分 |
| 无效点 > 80% | 30 分 |
| 噪声点 > 50% | 60 分 |
| 噪声点 > 80% | 30 分 |
| ICP 配准失败且 RMSE > 0.40 m | 30 分 |

> **对建图算法的启示**：精度（RMSE≤0.05m 满分）和完整度（覆盖全场）各占 40 分是大头；点云质量（无效点/噪声点）虽只 20 分但会引发封顶，**后处理被禁止，所以要在采集和配准阶段就控制质量，而不是事后清理**。

### 1.8 设备参数（第 23 页）

**四足机器人：**
- 站立尺寸 ≤ 630×360×415 mm；整机重量（含电池）≤ 15 kg
- 最大速度 ≥ 3.7 m/s；极限速度 ≥ 5 m/s；最大载荷 ≥ 8 kg
- 最大关节扭矩 ≥ 48 N·m；续航 1~2 h
- 相机像素 ≥ 800 万；广角视场角 DFOV ≥ 122°
- 标配 8 核 CPU；防护等级 ≥ IP54；电池电压 ≤ 43.2 V

**激光发射器：**
- 激光头 650 nm / 5 mW / 绿光；室内射程 0~150 m
- 工作电压 DC 12V；控制方式：红外遥控 / ZSL 接口
- 报靶方式：**语音报靶**（以语音反馈为准）
- 安装于机器人本体；供电接口 12V × 1

**靶面：**
- 尺寸 540×540 mm；材质金属+亚克力
- AprilTag 固定靶置于场地内

### 1.9 SDK 协议与网络（第 21、22 页）

- **Matrix 仿真中，SDK 控制机器人与读取仿真环境通过 Zenoh 协议**
- **仿真与真机切换**：需更改 `sdk_config.yaml` 中 `target_ip`，与 SDK 程序中的 ip 设置
  - 仿真常用 IP：**127.0.0.1**
  - 实机常用 IP：**192.168.234.X**
- **sdk_config.yaml 位置：`robot_mc/build/export/config/sdk_config.yaml`**
- 项目结构（22 页）：`config/`（仿真配置）、`deps/`（依赖与 SDK）、`rviz/`、`scripts/`、`src/UeSim`（UE 仿真）、`src/robot_mc`（运动控制）、`src/robot_mujoco`（MuJoCo 物理）、`run_sim.sh`（启动主脚本）

### 1.10 禁止事项汇总

| 编号 | 约束 |
|------|------|
| C1 | 禁止剪辑、拼接、删减视频 |
| C2 | 禁止对结果人工修改、二次加工 |
| C3 | 仅收取原生 PCD（未经后处理）|
| C4 | 禁止人工瞄准或人工控制（任务三）|
| C5 | 不得跨校组队 |
| C6 | 不得随意更换队伍成员 |
| C7 | 每名学生限报 1 队 |
| C8 | 同一学校限 5 支队伍 |
| C9 | 每队限 3 名学生、1 名指导教师 |
| C10 | 只能使用 SDK 控制机器人（不得绕过 SDK 直接改仿真内核）|
| C11 | 比赛固定使用钢镚 L1（xgb）机器人 |
| C12 | 未到现场者取消资格 |

### 1.11 赛事联系方式

- 官方网站：www.robotac.cn
- QQ 群：1004520666
- 赛务微信：robotac2015
- 电子邮箱：robotac@126.com

### 1.12 硬性技术指标

- 平台：MATRiX = **MuJoCo + Unreal Engine 5 + CARLA**；Ubuntu 22.04；ROS 2 **Humble**
- GPU：**NVIDIA RTX 4060 及以上**；驱动 **≥ 535**
- 机器人：固定 **钢镚 L1（xgb 型号）**（SDK 里还有 xgw/zgws/go2 等型号，比赛只用 xgb）
- 比赛使用**比赛特定地图**（非平台标准地图，需针对官方比赛地图训练）
- 传感器：RGB 1920×1080@10Hz；深度 640×480@10Hz；Mid360 激光雷达@10Hz；IMU + odom
- Topic：`/front_camera/image/compressed`、`/front_depth/image/compressed`、`/front_lidar`（PointCloud2）

---

## 2. 技术环境踩坑（项目 md 文档）

### 2.1 部署与启动

| 坑 | 详情 | 应对 |
|----|------|------|
| README 目录名不一致 | 文档写 `cd matrix_robotac`，实际目录是 `matrix_robotac_first` | 按实际目录进入 |
| Dockerfile 引用不存在文件 | 执行 `./build.sh` 和 `CMD ./open_sim_launcher` 均不存在 | 直接 docker build 会失败，改用 `./scripts/build.sh`；容器起来手动执行 `./run_sim.sh` |
| build.sh 能力不符 | README 说"自动装所有依赖"，实际只装系统依赖，UeSim 不下载、MC/MuJoCo/Navigo 不编译 | UeSim 需单独 `scripts/download_uesim.sh`（Google Drive，国内网络可能不可用）|
| 过时脚本引用不存在目录 | `modify_config.sh`/`build_mc.sh`/`build_mujoco_sdk.sh`/`build_navigo.sh` 引用 `jszr_*`/`navigo`，实际目录是 `robot_mc`/`robot_mujoco` | 这些脚本已过时（好在 build.sh 里被注释）|
| run_sim.sh 有启动锁 | `/tmp/run_sim_$(id -u).lock`，`flock -w 60`；并发启动会阻塞 60 秒后失败 | 不要并行启动 |
| run_sim.sh 会 pkill | 按进程名杀掉 `robot_mujoco/jszr_mujoco_ue/UnrealGame/UE4Editor/mc_ctrl` | 可能误伤同名进程 |
| 场景映射异常 | SCENE_ID 0/1/2 **全部映射 Level_2**，只有传未知值才落 Level_1；默认 SCENE_ID=1 | 默认启动的是 Level_2，关卡地图需确认 |
| 用 sed 原地改配置 | 每次运行改写 config.yaml、config.json（还追加了逗号）、run_mc.sh、xg-user-parameters.yaml | 反复运行会累积脏配置；脚本只适配 `xgb`，传其他值直接 exit 1 |
| 机器人出生 z 硬编码 | config.json 的 position.z 被忽略，XML `base_link` z 写死 **0.65** | 改出生高度要改 XML 里的 0.65 |
| MuJoCo 需要 license | `MUJOCORUNNING=1` 会启动 MuJoCo 物理引擎，需合法 license | 默认 `MUJOCORUNNING=0`（纯 UE 运动学）|
| run_mc.sh 参数 | usage 写 `{s r}` 但只有 `r` 分支 | 只传 `r` |
| taskset 绑定 CPU7 | `taskset -c 7` 要求机器 ≥8 核 | 核数不足会导致运控启动异常 |
| 启动时序固定 sleep 5 | UE 启动后固定睡 5 秒再启动 MC | 机器慢时 MC 报连接失败，可调大 sleep |
| PixelStreaming 地址 | `-PixelStreamingURL=ws://127.0.0.1:8888` | 远程访问需改 IP |
| install_deps.sh 环境限制 | 只适用 Ubuntu 22.04 + x86_64；deb 为 jammy/x86_64 | dpkg 缺依赖报错后执行 `sudo apt -f install` |

### 2.2 配置文件（config/*.json）

| 坑 | 详情 | 应对 |
|----|------|------|
| 深度 Topic 后缀不一致 | `config.json` 是 `/front_depth/image/compressed`，`config_default.json`/`config_xxg.json` 是 `/front_depth/image`（无 /compressed）| 订阅代码要与你实际使用的 config 一致 |
| robot_type 不一致 | `config.json`=xgb，`config_default.json`/`config_xxg.json`=xxg | run_sim.sh 只支持 xgb |
| **深度不是点云** | `cloudmode: false` → 深度传感器只发压缩图像，不发点云 | 建图点云需用深度图+相机内参反投影，或依赖 LiDAR 点云 |
| lidar 参数 | mid360，`draw_points:false`、`random_scan:false` | 与 Topic 数据相关，改动需谨慎 |
| 特殊场景模板 | `config_multi_robot.json`（顶层 robot1/robot2）、`config_panorama.json`、`config_wideanglecamera.json` 结构不同 | 不要直接当主配置用 |

### 2.3 SDK 部署（deploy.md）

- **版本强绑定**：zsl-1：SDK≥0.2.6 + 运控≥0.2.6（本体≥0.2.0）；zsl-1w：SDK≥0.2.7 + 运控≥0.3.1（本体≥0.3.3）。版本不匹配协议不兼容
- **网络**：WiFi 默认 IP **192.168.234.1**（有 DHCP，控制端不能设固定 IP）；有线 **192.168.168.168**（无 DHCP，控制端需手动设 168 网段固定 IP）
- SSH 登录：`ssh firefly@{IP}`，用户名/密码都是 **firefly**
- **必须修改机器人端 `/opt/export/config/sdk_config.yaml` 的 `target_ip` 为控制端 IP**（target_port=43988），改完**必须重启设备**
- **`SDK_CLIENT_IP`**（非 WiFi 网段必配）：与 target_ip **必须同网段**；⚠️ 危险：配置后有概率导致机器人无法用遥控器
- ⚠️ 不要删除/注释 sdk_config.yaml 里的 ip 行，否则运控服务无法自起
- ⚠️ 程序/运控更新后**配置会重置**，需重新配置
- **demo 的 IP/端口必须与 sdk_config 一致**：C++ `CLIENT_PORT=43988`、`CLIENT_IP="192.168.234.15"`；Python `app.initRobot("192.168.234.15", 43988, "192.168.234.1")`（本地IP, 本地端口, 机器人IP）
- 库路径 `lib/zsl-1/${ARCH}`，架构识别失败会 FATAL_ERROR；zsl-1w 无 LowLevel

### 2.4 SDK 使用（faq.md）

- ⚠️ **电量低会直接断电**（SDK 无保护），控制前务必关注电量
- WiFi 干扰：建议算力板+网线直连
- `connect time out` 排查顺序：①改 target_ip ②非 WiFi 直连改 SDK_CLIENT_IP ③检查运控绑定 IP
- 运控绑定 IP 失败排查：`sudo lsof -i -P -n -c mc_ctrl | grep 43997`（注意是 **43997**，与 SDK 端口 43988 不同）；修复在 `/etc/systemd/system/robot-launch.service` 加 `Environment="ROBOT_NET_INTERFACES=wlan0"`（有线用 eth0）；⚠️ 配错且网卡无 IP 会导致机器狗程序无法启动
- wlan0 重启不自动连接：`sudo systemctl stop/disable networkmanager-cleanup.service`
- 手柄无法控制：①运控没启动 ②**SDK 运行中手柄失效**（SDK 优先级高于手柄）
- 频率：lowlevel 状态 500Hz、highlevel 50Hz；控制发送最大 500Hz，建议 20~50Hz
- ⚠️ **3 秒未收到 SDK 数据 → 自动切阻尼趴下**（控制循环间隔必须 < 3s）
- Highlevel 与 Lowlevel **不能同时使用**，切换需预留时间等端口释放
- IMU 是原始数据、精度受机型限制；欧拉角旋转顺序 **ZYX**；initRobot 卡住通常是 boost 版本问题（需 **boost 1.74.0**）
- SDK 本身暂无 ROS 接口（ROS2 通信是仿真平台侧提供的）

### 2.5 SDK API（api_zsl-1.md）

- ⚠️ **函数名以 `highlevel.h` 头文件为准**！文档里的 `standUP()`→实际 `standUp()`；`checkConnection()`→实际 `checkConnect()`；`cancleTwoLegStand()`→实际 `cancelTwoLegStand()`。照文档写会编译失败
- **move 速度范围（含死区）**：
  - vx：`-3 ~ -0.05 / 0.05 ~ 3` m/s
  - vy：`-1 ~ -0.1 / 0.1 ~ 1` m/s
  - yaw_rate：`-3 ~ -0.02 / 0.02 ~ 3` rad/s
  - 停止必须传 0；低于下限被判非法（返回 **0x3013 速度命令过大**）
- **错误码**：0 正常；0x3012 电机数据丢失；0x3010 电机失能；0x3011 电机故障；0x3009 电机角度超限；0x3007 状态机切换失败；0x3013 速度命令过大
- **状态机顺序（最核心）**：`standUp/lieDown/jump/frontJump/backflip/shakeHand` 等**移动中不可直接切入**，必须先 `move(0,0,0)` 停稳再切
- `twoLegStand` 退出必须调 `cancelTwoLegStand()`，且该状态下不响应其他接口
- `backflip/shakeHand/twoLegStand` 高频使用会加速电机/关节磨损
- `attitudeControl`：roll/pitch/yaw 速度 ±0.5 rad/s，height_vel ±0.5 m/s
- `getCurrentCtrlmode` 返回**最后一次执行状态**（不是实时状态）：0 阻尼、1 站立/打招呼、10 趴下短时后自由、18 移动、21 动作、51 趴下

### 2.6 SDK 架构（architecture.md）

- 指令下发需按**状态机跳转逻辑**，否则可能造成机器摔倒/故障/不响应
- 腿顺序：FR(右前)→FL(左前)→RR(右后)→RL(左后)；坐标系：前 X、左 Y、上 Z，右手系
- 头文件路径：`include/lowlevel/lowlevel.h`（不是文档写的 include/zsl-1/lowlevel.h）

### 2.7 版本变更（changelog.md）

- **v0.2.6 删除了一大批单轴接口**：`getRoll()/getPitch()/getYaw()/getAccelX()/getWorldVelZ()` 等已删，改用组合接口 `getQuaternion()/getRPY()/getBodyAcc()/getBodyGyro()/getPosition()/getWorldVelocity()/getBodyVelocity()`。老代码会编译失败，只使用当前 highlevel.h 中存在的接口
- v0.2.7 起 `checkconnect` 默认值为 **false**

### 2.8 视频流（video.md）

- RTSP 地址：标题写 `192.168.133.1:8554`，示例代码用 `192.168.234.1:8554/test`——**以 192.168.234.1:8554/test 为准**（WiFi 网段），按实际网络改 IP
- VLC 3.0.21 部分设备打不开，建议用 GStreamer；拉流有延迟需调缓冲/禁硬件加速

### 2.9 GPU / 容器

- NVIDIA 驱动 ≥ 535、RTX 4060+；`docker run --gpus all` 需 nvidia-container-toolkit
- 容器运行需 `--privileged`、`--network host`、挂载 `/dev/dri /dev/input /dev/uinput /dev/bus/usb`
- entrypoint 用 `set +u` 兼容 ROS source（ROS setup.bash 在 set -u 下报未定义变量）
- **⚠️ VMware 虚拟机跑不动 UE 仿真（2026-08-20 实测）**：UeSim 弹窗报 `Vulkan device could not be created at the project's supported feature levels`。原因：`UeSim/Config/DefaultEngine.ini` 写死 `TargetedRHIs=SF_VULKAN_SM6`，只认 Vulkan 1.3 + SM6 级设备；VMware 虚拟显卡（SVGA）达不到。**仿真必须在有 NVIDIA 独立显卡的物理机上跑 Ubuntu 22.04（或 GPU 直通），不要在 VMware 里跑 UeSim**。宿主机 RTX 4050 6GB 亦可（略低于官方 RTX 4060 要求）。排查命令：虚拟机内 `vulkaninfo --summary` 看设备；VMware 设置里「显示器 → 加速 3D 图形」需开启并装 `open-vm-tools-desktop`

🆕 新增至 2.9 GPU / 容器
笔记本双显卡（NVIDIA Optimus）UE 仿真启动踩坑（ASUS TUF FA507NU / RTX 4050 实测 2026-08-22） ⭐
表格
坑	现象	根因	解决
libmujoco.so.3.3.0: cannot open shared object file	UE 二进制启动即退出，run_sim.sh 立刻触发 cleanup；日志显示 MuJoCo will be disabled 但仍报错	MuJoCoUE 插件编译时硬链接了带版本号的 libmujoco.so.3.3.0，但项目自带库文件名为 libmujoco.so（无版本号后缀），动态链接器找不到	在库目录创建符号链接：cd src/UeSim/Linux/UeSim/Plugins/MuJoCoUE/Source/Binaries/Linux/ && ln -sf libmujoco.so libmujoco.so.3.3.0，然后 export LD_LIBRARY_PATH=$(pwd):$LD_LIBRARY_PATH
Vulkan device could not be created	UE 弹窗报错后崩溃（Saved/Config/CrashReportClient/ 下生成多个 UECC-Linux-* 目录）	Wayland 会话下 NVIDIA Vulkan ICD 无法正确初始化；或 .bashrc 中写死 VK_ICD_FILENAMES 指向错误路径导致 ICD 枚举失败	① 确认 X11 会话：echo $XDG_SESSION_TYPE 应为 x11；② 不要在 .bashrc 中写死 VK_ICD_FILENAMES，改用 PRIME offload 标准方式启动（见下方命令）；③ 系统默认 Vulkan 枚举正常时勿强制指定 ICD
启动命令写法	直接 ./run_sim.sh 走集显，UE 渲染异常或帧率极低	笔记本默认使用 AMD 核显渲染，未调用 NVIDIA 独显	使用完整 PRIME offload 前缀启动：
__NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia __VK_LAYER_NV_optimus=NVIDIA_only ./run_sim.sh xgb 1 0 0 0
永久生效配置（写入 ~/.bashrc）：
bash

# MuJoCo 库路径（符号链接创建后）

export LD_LIBRARY_PATH=~/matrix_robotac_worksapce/matrix_robotac_first/src/UeSim/Linux/UeSim/Plugins/MuJoCoUE/Source/Binaries/Linux:$LD_LIBRARY_PATH


# ⚠️ 不要添加以下两行（已验证会导致 Vulkan 枚举失败）：

# export __VK_LAYER_NV_optimus=NVIDIA_only    ← 仅在启动命令中临时使用

# export VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json  ← 删除此行

验证命令速查：
bash

# 确认 X11 + NVIDIA Vulkan 正常

echo $XDG_SESSION_TYPE          # 应输出 x11

vulkaninfo --summary 2>&1 | grep deviceName   # 应包含 "NVIDIA GeForce RTX 4050"


# 确认 libmujoco 链接正确

ldd src/UeSim/Linux/UeSim/Binaries/Linux/UeSim | grep mujoco

# 应显示 libmujoco.so.3.3.0 => .../libmujoco.so.3.3.0（非 "not found"）

    💡 核心教训：UE 启动即崩且 run_sim.sh 秒退时，优先检查 UeSim.log 首行的 error while loading shared libraries——这比 CrashReport 更能直接定位问题。Vulkan/GPU 问题与缺库问题症状相似但根因完全不同，需分别排查。

### 2.10 云端 GPU 服务器（AutoDL 实测 2026-08-20）⭐部署成功

**可行方案：AutoDL 优云算 RTX 3080 Ti 12GB + Ubuntu-nvidia 22.04 镜像（¥0.74/时）**

- **镜像**：选「系统镜像 → Ubuntu-nvidia 22.04」（不要选 PyTorch/CUDA 基础镜像，与 UE5 图形驱动冲突风险高）
- **GPU**：3080 Ti 12G 足够（Vulkan 1.4.303 远超 SM6；Ampere 支持完整）
- **防火墙**：选「非Web服务器推荐」（22/3389）
- **部署步骤**：SSH → 装 ROS2 Humble → 上传代码 → `source /opt/ros/humble/setup.bash && ./run_sim.sh xgb 1 1 0 0`（离屏渲染，勿开 PixelStreaming）

**云端部署踩坑汇总：**

| 坑 | 现象 | 解决 |
|----|------|------|
| sources.list.d 缺失 | `Unable to locate package ros-humble-desktop` | AutoDL 镜像删了该目录，先 `mkdir -p /etc/apt/sources.list.d` 再写 ros2.list |
| apt 锁占用 | `Could not get lock /var/lib/dpkg/lock-frontend` | 旧 apt-get 卡在后台下载，`pkill -9 apt-get + rm 锁文件` 后重试 |
| UeSim 拒绝 root | `Refusing to run with the root privileges` | **必须普通用户启动**，勿用 sudo 跑 run_sim.sh |
| libfastcdr 缺失 | `cannot open shared object file: libfastcdr.so.1` | 启动前 `source /opt/ros/humble/setup.bash` 或 `export LD_LIBRARY_PATH=/opt/ros/humble/lib` |
| run_sim 启动锁 | 第二次启动等 60s 后失败 | flock 锁 `/tmp/run_sim_$(id -u).lock`，先杀旧进程再启 |
| **PixelStreaming 不可用** | `-RenderOffScreen + -PixelStreamingURL` 或 xvfb+PS 均卡死：渲染线程 `Ensure condition failed: OriginX <= OriginMax` 死循环（UeSim 空转，8888 不监听）| **放弃 PixelStreaming**，改用「服务器 `ros2 bag record` 录制 → 本地 rviz2 回放」（指南：`ROS2_bag录制回放指南.md`）|

**云端仿真已验证 Topic**：/front_lidar(10Hz) /front_camera/image/compressed(10Hz) /front_depth/image/compressed /imu(496Hz) /odom/mujoco_odom /fire 等

**费用提醒**：按小时计费，不用时关机（AutoDL 支持无卡开机保留数据）。

### 2.11 本机仿真 + 建图实测结论（2026-08-22，全部实测验证）⭐

**SDK 控制链路三件套（缺一不可，"connect time out" 逐项排查）：**

1. **5 个 deb 依赖必须装**：`sudo bash scripts/install_deps.sh`（lcm/zsibot_common/ecal/mujoco/onnx）。双系统新装机器全部缺失，mc_ctrl 启动即报 `liblcm.so.1: cannot open shared object file` 退出
2. **sdk_config.yaml 的 target_ip 改 127.0.0.1**（`src/robot_mc/build/export/config/sdk_config.yaml`，出厂是 192.168.234.1）
3. **run_mc.sh 加 `export SDK_CLIENT_IP=127.0.0.1`**（⚠️ 最容易漏）：不加则 mc_ctrl 的 SDK 模块默认绑定 192.168.234.1:43997 → `bind: Cannot assign requested address` → `sdk init failed!`。非 WiFi 网段必配，见 faq.md
- 验证：mc_ctrl 日志出现 `client_ip: 127.0.0.1` 且无 bind 报错；`ss -unp \| grep 43997`（SDK 协议是 UDP，查 TCP 看不到）

**建图实测结论（配准已不是瓶颈）：**

| 结论 | 证据 | 对代码的要求 |
|------|------|-------------|
| odom 无时间滞后 | 原地旋转实测 τ≈-2ms，转弯帧 ICP 修正仅 0.1°/帧 | 纯 odom 配准即可，**不要用 ICP**（实测注入最大 25cm 漂移）|
| 迷宫墙真实厚度 ~12cm | 墙线对间距恒定 10~14cm、位置漂移仅 1~4cm | 地图里"双线"是墙的两面，**是正确内容**，不是重影错误 |
| 仿真 lidar 系统性散射 ~2cm/线 | 70 次观测平均后 2.04 vs 2.11cm | 体素平均降噪**无收益**，keep-first 即可 |
| 逐点 timestamp 全为 0 | 实测 | 无帧内畸变，不需要去畸变；FAST-LIO2 等依赖逐点时间戳的方案在本仿真环境失效 |
| 野点 0.56%（z 达 ±29m）+ 远点精度差 | 实测（近 7cm→远 17cm）| 必须 z 过滤（[-0.5,4]m）+ 限距（15m 内点精度好，远处计入噪声点扣分）|
| UeSim 传感器是 **Best Effort QoS** | rosbag2 回放拒绝 Reliable 订阅 | ROS2 订阅必须 `ReliabilityPolicy.BEST_EFFORT`，否则收不到数据 |
| 建图节点 /slam_map 是 **RELIABLE**（rclpy 默认 QoS）| `ros2 topic info /slam_map -v` 订阅数=0，rviz 绿色地图不显示 | rviz 里 SLAMMap 显示的 QoS 必须设 **Reliable**（slam_view.rviz 已改）；rviz 两个点云显示的 QoS 相反是正常的，各自与发布方匹配即可 |
| 无 TF 发布 | 实测：rviz 报 `Frame [lidar] does not exist`、点云全部不渲染 | 建图节点已内嵌 TF 发布：静态 base_link→lidar（mount 偏移）+ 动态 odom frame_id→child_frame_id（world→base_link）；rviz Fixed Frame 用 `world` 即可 |

**本工作区新增工具（`matrix_robotac_first/user_code/`）：**

| 文件 | 用途 |
|------|------|
| `teleop_keyboard.py` | 键盘遥控（WASD 动/Space 停/L 趴下/Q 退出；20Hz 持续发命令防 3 秒趴下）|
| `slam_odom_mapper.py` | 建图节点：纯 odom 配准 + z 过滤 + 限距 15m（ICP 默认关，--icp 1 可试验）；订阅 lidar+odom → /slam_map → Ctrl+C/--save-after 存 PCD；**并发布 TF（world→base_link→lidar）供 rviz 渲染原始 lidar 点云**；另发布 /map_2d 2D 占据栅格（空闲=机器人经过 0.3m 圆盘，墙=≥2 次命中，供 frontier 探索） |
| `slam_view.rviz` | 建图 rviz 配置（Fixed Frame=world，含 /slam_map 绿色显示）|
| `auto_explore.py` | 左手规则墙跟随 v2：窄前扇区 12° + 前墙多帧确认 + PD 阻尼贴墙 + 弧线转弯不停车 + 速度随前距自适应（解决直道误刹/转弯费时/原地踏步晃点云）|
| `frontier_explore.py` | Frontier 前沿探索（Yamauchi 1997）：订阅 /map_2d + odom + lidar → 最近前沿 A* 路径 → 纯追踪；途中重校验目标（Holz 改进）、卡住封锁目标 60s、无前沿自动完成并报覆盖率 |

用法：`python3 slam_odom_mapper.py --out map.pcd --save-after 1500`；bag 回放加 `--ros-args -p use_sim_time:=true`（回放器订阅需 Best Effort，本地录制的 bag 同理）。

---

## 3. 代码开发注意事项（demo 代码 + API 实测）

### 3.1 SDK 导入路径（Python）

```python
import os, platform, sys
arch = platform.machine().replace('amd64', 'x86_64').replace('arm64', 'aarch64')
lib_path = os.path.abspath(f'{os.path.dirname(__file__)}/../../../../lib/zsl-1/{arch}')
sys.path.insert(0, lib_path)
import mc_sdk_zsl_1_py   # 库名
app = mc_sdk_zsl_1_py.HighLevel()   # 类名 HighLevel
```

### 3.2 initRobot 参数（仿真 vs 真机）

```python
# 仿真环境（本机）：
app.initRobot("127.0.0.1", 43988, "127.0.0.1")   # (local_ip, local_port, dog_ip)
# 真机：
app.initRobot("192.168.234.15", 43988, "192.168.234.1")
```

> 仿真固定 127.0.0.1；真机 IP 见 2.3。**切换真机必须同时改 sdk_config.yaml（机器人端）和 initRobot（代码端）**。

### 3.3 高频调用顺序示例（demo 验证）

```python
app.standUp()          # 先站立
time.sleep(4)
app.move(0.1, 0, 0)    # 再移动（速度要避开死区，见 2.5）
```

### 3.4 其他代码层注意

- Python LowLevel 与 C++ 头文件字段可能不一致（以具体 demo 和头文件为准）
- 订阅深度 Topic 前先用 `ros2 topic list` 确认实际话题名（rviz 配置与 config 可能不一致，见 2.2）
- 3 秒无数据趴下 → 控制循环间隔 < 3s
- 端口/IP 三处必须一致：机器人端 sdk_config.yaml + SDK_CLIENT_IP + demo initRobot

---

## 4. Top 20 最高优先级清单（速查表）

| # | 注意点 | 风险 |
|---|--------|------|
| 1 | 函数名以 `highlevel.h` 头文件为准（standUp/checkConnect/cancelTwoLegStand）| 编译失败 |
| 2 | 状态机顺序：移动中不可直接切 standUp/lieDown/jump 等，先 move(0,0,0) 停稳 | 摔倒/故障 |
| 3 | 3 秒无 SDK 数据自动趴下 → 控制循环 < 3s | 任务中断 |
| 4 | 端口/IP 三处一致（sdk_config.yaml + SDK_CLIENT_IP + initRobot）| 连不上 |
| 5 | 仿真 initRobot 填 127.0.0.1；真机填 192.168.234.X | 连不上 |
| 6 | 深度数据是压缩图像不是点云（cloudmode:false），建图需自己转 | 建图无数据 |
| 7 | PCD 禁止后处理，仅原生输出；按工位号命名 | 取消资格 |
| 8 | 视频禁止剪辑，mp4 ≤15min ≤500MB | 拒收 |
| 9 | 中期检查 9 月 12 日前（硬 deadline）| 淘汰 |
| 10 | 建图评分 100 分制：精度 40（RMSE≤0.05m）+ 完整度 40 + 质量 20 | 分数上限 |
| 11 | move 速度死区（vx 0.05 / vy 0.1 / yaw 0.02），停止传 0 | 报错 0x3013 |
| 12 | 错误码判断（0x3012/3010/3011/3009/3007/3013）| 调试困难 |
| 13 | Dockerfile 的 ./build.sh 和 ./open_sim_launcher 不存在 | 部署失败 |
| 14 | run_sim.sh 只支持 xgb；SCENE_ID 0/1/2 全映射 Level_2 | 场景不符 |
| 15 | MuJoCo 需要 license，默认关闭（MUJOCORUNNING=0）| 启动失败 |
| 16 | 电量低直接断电（实机）| 设备损坏 |
| 17 | SDK 运行中手柄失效（实机）| 失控 |
| 18 | 更新后 sdk_config 配置被重置（实机）| 连不上 |
| 19 | v0.2.6 删除了单轴接口，只用组合接口（getQuaternion/getRPY 等）| 编译失败 |
| 20 | 任务三禁止人工瞄准/控制，全自动 + 脱靶反馈修正 | 违规 |

### 4.1 Top 补充（2026-08-22 实测新增，优先级同 Top 20）

| # | 注意点 | 风险 |
|---|--------|------|
| 21 | 双系统新装机必须 `sudo bash scripts/install_deps.sh` 装 5 个 deb | mc_ctrl 起不来 |
| 22 | run_mc.sh 必须 `export SDK_CLIENT_IP=127.0.0.1`（仿真），否则 SDK 模块绑定 192.168.234.1:43997 失败 | connect time out |
| 23 | 建图**不要开 ICP**（--icp 默认 0）：odom 无滞后、ICP 注入漂移 | 地图重影 |
| 24 | 地图"双线"= 迷宫墙真实双面（厚 ~12cm），是正确内容 | 误判为 bug 白折腾 |
| 25 | 建图节点订阅必须 Best Effort QoS，否则收不到 UeSim 数据 | 无点云 |
| 26 | 建图必须 z 过滤（-0.5~4m）+ 限距 15m（0.56% 野点 z ±29m；远点精度差计噪声点） | 质量分被扣/封顶 |
| 27 | 仿真 lidar 逐点 timestamp 全 0 → 无需去畸变，依赖逐点时间的方案（FAST-LIO2 等）失效 | 选错算法 |
| 28 | rviz 报 `Frame [lidar] does not exist`：仿真无 TF 树，Fixed Frame/原始 lidar 显示都无法解析坐标系；由建图节点发布 TF（需保持其运行），重启建图节点后等 1~2 秒即恢复 | 看不见点云 |
| 29 | /slam_map（发布 RELIABLE）与 rviz SLAMMap 显示（曾配 Best Effort）QoS 不匹配 → 订阅数 0、绿色地图不显示；rviz 显示改 Reliable 即可；验证：`ros2 topic info /slam_map -v` 看订阅数 | 看不见地图 |
| 30 | rviz 用 `-d` 相对路径加载配置：从错误目录启动时配置静默丢失（无 SLAMMap 显示、无订阅）；必须用绝对路径或先在 `matrix_robotac_first/` 目录启动 | 看不见点云 |
| 31 | 自主遍历两代方案：墙跟随 v2（稳）→ frontier 探索（覆盖全）；/map_2d 与 /slam_map 都是 RELIABLE（rclpy 默认），订阅方用默认 QoS 即可；一次只跑一个控制程序（端口 43988 互斥）| 程序互斥冲突 |

---

## 5. 附录：文档与代码来源

| 来源 | 路径 |
|------|------|
| 赛题 PDF（25 页）| `E:\ROBOTAC\matrix_robotac_worksapce\rules.pdf`（同《第二十五届...赛题说明.pdf》）|
| 项目根 | `E:\ROBOTAC\matrix_robotac_worksapce\matrix_robotac_first\` |
| SDK 文档 | `matrix_robotac_first\deps\zsibot_sdk\docs\`（deploy/api_zsl-1/api_lowlevel/architecture/faq/changelog/video/sdk_download）|
| SDK 示例 | `matrix_robotac_first\deps\zsibot_sdk\demo\zsl-1\python\examples\`、`cpp\` |
| SDK 头文件 | `deps\zsibot_sdk\include\zsl-1\highlevel.h`（**以此为准**）、`include\lowlevel\lowlevel.h` |
| 仿真配置 | `matrix_robotac_first\config\*.json` |
| 启动脚本 | `matrix_robotac_first\run_sim.sh`、`scripts\` |
| 本队代码 | `matrix_robotac_first\user_code\`（teleop_keyboard.py / slam_odom_mapper.py / slam_view.rviz，见 2.11）|
| 官网 | www.robotac.cn |
