# 队友复现指南（从零到 SLAM 建图跑通）

> 目标：在新机器上复现当前 FAST-LIO2 建图效果。仓库不含官方平台本体（7.6GB），
> 平台需从比赛官方渠道获取。本文档配套：`SLAM启动手册.md`（SLAM 流程）、
> `注意事项.md`（平台部署踩坑全集，**必读**）。

## 0. 前提条件

- Ubuntu 22.04 双系统，**NVIDIA 独立显卡**（RTX 4050+；VMware 虚拟机跑不了 UeSim，Vulkan 不支持）
- GitHub 权限：让队长在仓库 Settings → Collaborators 添加你的账号

## 1. 拿代码 + 拿平台

```bash
# 1. clone 仓库（私有，需已加为 collaborator）
mkdir -p ~/matrix_robotac_workspace && cd ~/matrix_robotac_workspace
git clone https://github.com/Wuu-J/new_robotac.git .
# 2. 官方渠道下载 MATRiX 平台压缩包（约 7.6GB），解压后把内容放进 matrix_robotac_first/
#    即与仓库里的 matrix_robotac_first/user_code 合并成完整目录：
#    matrix_robotac_first/
#      ├── user_code/        ← 仓库里的本队代码（保留）
#      ├── src/ deps/ config/ scripts/ run_sim.sh ...   ← 平台本体
```

## 2. 平台部署三件套（关键，详见 注意事项.md 2.11）

```bash
# ① 装 5 个系统依赖（mc_ctrl 缺了就起不来）
cd ~/matrix_robotac_workspace/matrix_robotac_first
sudo bash scripts/install_deps.sh
# ② sdk_config.yaml 的 target_ip 改 127.0.0.1
#    src/robot_mc/build/export/config/sdk_config.yaml
# ③ run_mc.sh 加 export SDK_CLIENT_IP=127.0.0.1（最容易漏）
# ④ libmujoco 符号链接（注意事项 2.9）：
#    cd src/UeSim/Linux/UeSim/Plugins/MuJoCoUE/Source/Binaries/Linux/
#    ln -sf libmujoco.so libmujoco.so.3.3.0
#    并把该目录加入 ~/.bashrc 的 LD_LIBRARY_PATH
```

**验证平台**（笔记本双显卡必须带 PRIME offload 前缀）：
```bash
cd ~/matrix_robotac_workspace/matrix_robotac_first
__NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia __VK_LAYER_NV_optimus=NVIDIA_only ./run_sim.sh xgb 1 0 0 0
# 另开终端确认：
source /opt/ros/humble/setup.bash && ros2 topic list | grep -E 'front_lidar|/imu'
```

## 3. SLAM 依赖 + 编译（源码全在仓库 slam_ws_src/）

按 `SLAM启动手册.md` 的「一次性环境准备」执行 5 步：
```bash
# ① apt 依赖
sudo apt install -y libceres-dev libgflags-dev libatlas-base-dev ros-humble-pcl-ros ros-humble-gtsam
# ② Livox-SDK2（sudo make install 到 /usr/local）
# ③ Sophus 1.22.10（本地前缀，路径改成自己的家目录）
# ④ 编译 livox_ros_driver2（源码 cp 自 slam_ws_src/）
# ⑤ 编译 FASTLIO2_ROS2（源码 cp 自 slam_ws_src/，配置已适配仿真话题与外参）
```

⚠️ 启动手册里的 `/home/stylerobotac/...` 路径全部替换成你自己的家目录。

## 4. 启动 + 回环验证 + 保存

完全按 `SLAM启动手册.md` 的「每次开机启动流程」（6 个终端）和「回环验证要点」「保存与合并」执行。

## 5. 队友最可能踩的坑（按出现频率排序）

| # | 坑 | 症状 | 解法 |
|---|---|---|---|
| 1 | 笔记本没加 PRIME offload | UE 用集显渲染，帧率极低或 Vulkan 报错 | 启动仿真命令带三前缀（见上） |
| 2 | VMware/无独显 | `Vulkan device could not be created` | 换双系统 + N 卡物理机 |
| 3 | run_mc.sh 漏 SDK_CLIENT_IP | 遥控 connect time out | 注意事项 2.11 ③ |
| 4 | 漏装 5 个 deb | mc_ctrl 报 liblcm.so.1 缺失退出 | sudo bash scripts/install_deps.sh |
| 5 | 缺 libmujoco.so.3.3.0 符号链接 | UE 启动即退 | 注意事项 2.9 |
| 6 | 仿真重启后 LIO 发散 | 位置跳到几千、NO Effective Points | 重启 LIO 节点（桥接不用） |
| 7 | Sophus 头文件找不到（hba 编译失败） | sophus/so3.hpp No such file | 编译参数带 -DCMAKE_CXX_FLAGS=-I<你的>/fastlio2_deps/sophus/include |
| 8 | 长命令粘贴断行 | command not found / Is a directory | 整行一次粘贴 |
| 9 | 两个 SDK 客户端互抢 | 遥控无响应 | 一次只跑一个（43988 互斥） |
| 10 | 快速转圈建图拉花 | 1 rad/s+ 转圈航向欠转 | 建图时转向 ≤0.4 rad/s |

## 6. 与队长的协作约定

- 中期检查红线：PCD 为算法原生输出（禁事后加工）、视频一镜到底、9 月 12 日前提交
- 所有改动走 git：`git pull` 同步最新，改完 `git push origin main`
