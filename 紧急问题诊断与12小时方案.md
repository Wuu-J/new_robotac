# 建图问题完整诊断与 12 小时应急方案

> 日期: 2026-08-25 01:30（距中期检查提交约 12 小时）
> 状态: 提交图 = `save_run.sh` 生成的 `run_时间戳/merged.pcd`

---

## 一、当前系统配置（锁定状态）

| 组件 | 配置 | 备注 |
|---|---|---|
| LIO (fastlio2) | **14:44 实测版**：near_search_num=5、无 z-prior、无 NaN 防护、t_wi 雅可比 | 用户实测 rviz 点云最干净的一版 |
| builder (lio_map_builder.py) | keep-first + 地面 hits≥1 + 孤立剔除 + 门控（**本轮用 `--guard 0` 关掉**） | 门控会因角落 z 漂移冻结地图 |
| PGO | 手动时间戳配对 + 回环**关闭** + NaN 拒收 + yaml 修复 | 关键帧全程录制已修复 |
| merge | 字段自适应读取 + 逐 patch 地面对齐（中位数参考）+ 发散丢弃(>0.5m) + 时间共识 + 孤立剔除 | 提交图加工链 |

---

## 二、问题清单与根因链（全部实测定位，非猜测）

### 问题 A：三面墙角落 z 漂移（14:44 版的已知弱点）

**现象**：机器人进三面墙角落时，LIO 高度估计漂移（0.3~0.5m，严重时跑飞至 10m）。

**根因链**：
```
角落场景点面残差秩亏
  → IESKF 信息矩阵 H 在 z/横切方向接近奇异（数学上不可观）
  → z 方向只靠 IMU 积分维持 → 加速度零偏 ba_z 的误差持续积分
  → z 单调漂移，激光地面点对不上 → 正反馈 → 跑飞
```
这是 FAST-LIO2 的**已知系统性缺陷**，联网搜索确认（见第四节）：温室场景 z 漂移可达 2.08m。

**我们试过的**：
- z-prior 软约束（1e4）——能钉住 z，但疑似通过奇异 H⁻¹ 耦合把 z 残差泄漏进 x/y 更新，导致 x/y 偏移/重影 → 已回退
- near_search_num 5→8——无效
- 门控冻结——保护地图但"点云半路没了"，本轮已关

**当前兜底**：merge 管线逐 patch 地面对齐（把每个关键帧的 z 按自身地面拉回 0）+ 发散 patch 丢弃 → **提交图（merged）不受角落 z 漂移影响**。

### 问题 B：x/y 往返偏移 ~0.3m（重影）

**现象**：同一条走廊去程回程各扫一遍时，同一面墙出现两条相隔 ~0.3m 的平行线。

**根因链**：
```
无回环约束的里程计纯累积：转弯时 IESKF 航向欠转（实测快转 1-11%）
+ 位置微漂（转圈时 ≤0.26m）
  → 折返时位姿与去程错位 ~0.3m
  → 同一面墙被投到两个位置（平行双线）
```
这是**无回环 LIO 的正常漂移量级**（0.3m / 60m 路程 = 0.5%）。

**当前对策**：
1. **驾驶纪律**：每条走廊只走一遍、不折返（双线只出现在重访处）
2. PGO 回环（本应吸收此误差）——从未成功运行过（见问题 C）

### 问题 C：PGO 回环从未成功（关键帧曾反复停录）

**现象**：merged 缺大半区域——关键帧录制跑一半就停。

**根因链（三个叠加 bug，全部实测定位并修复）**：
```
① message_filters ApproximateTime 同步器运行约 35s 后静默饿死
   （两话题持续 10Hz 正常流动、时间戳一致，日志 12 分钟无新 sync）
   → 修复：改为手动时间戳配对（|dt|<50ms，与 builder 同款）
② syncCB 乱序拒绝：仿真时钟一次超前跳变后所有消息被永久丢弃
   → 修复：删除该守卫
③ ICP/GTSAM 挂起：进程睡眠态/单核满转、日志静止（回环路径）
   → 对策：enable_loop=false 关闭回环 + try/catch 保护
④ pgo.yaml 注释粘连数值行 → YAML 解析异常 → 节点启动即死
   → 修复：拆行（2026-08-25 01:0x）
```
**现状**：回环关闭状态下关键帧全程录制（实测 keyframes 持续增长），merged 完整性由关键帧保证。

### 问题 D：z-prior 实验失败（已回退）

**现象**：加了 z-prior 后 rviz 出现 x/y 偏移/重影，比 14:44 差。

**分析**（假设，未完全验证）：z-prior 只改 H[5][5]，但在角落奇异 H 下，H⁻¹ 的耦合项会把 z 残差（步态颠簸 ±5cm × 权重 1e4）泄漏进 x/y 更新。与 near_search_num=8 同时改动，变量混淆，未能单独验证。

**结论**：LIO 完全回退 14:44；z 问题改由 merge 管线的逐 patch 对齐兜底。

### 问题 E：已修复的工程 bug 汇总

| bug | 影响 | 状态 |
|---|---|---|
| merge 按 4 字段读 8 字段 patch | merged 少一半点 | ✅ 按 header 字段解析 |
| 地面 min_hits=2 | 地面点 -64% | ✅ 地面带 hits≥1 |
| 位姿跳变幽灵帧 | 地图外散点 | ✅ 门控（本轮关闭，靠孤立剔除兜底） |
| merge 无孤立剔除 | 提交图散点 | ✅ 26 邻域连通性剔除 |
| PGO 地图系地面 z≈-0.65 被误判为漂移 | patch 误丢弃 | ✅ 中位数参考自适应 |

---

## 三、已尝试方案与结果

| 方案 | 结果 |
|---|---|
| 门控（跳变+速度+z带） | 有效但冻结副作用大；本轮 `--guard 0` 关闭 |
| 质心平均累积（替代 keep-first） | LIO 漂移时把旧墙拉歪，重影加重 → 回退 keep-first |
| 地面 min_hits=1 | 地面 +177%，墙面零影响 ✅ 保留 |
| 孤立点剔除（builder+merge） | 实测清 885 离散体素，结构无损 ✅ 保留 |
| z-prior | 钉住 z 但疑似引入 x/y 问题 → 回退 |
| 旋转雅可比 t_wi→t_il 修正 | 推导正确但实测更差 → 回退 t_wi |
| near_search_num 5→8 | 角落无改善 → 回退 5 |
| 逐 patch 地面对齐 + 发散丢弃 | 修复高度分裂、清悬挂点 96% ✅ 保留 |
| PGO 手动配对 / 关回环 / NaN 拒收 | 关键帧全程录制 ✅ 保留 |

---

## 四、联网搜索结果（共识解法）

1. **地面约束是 z 漂移的标准解法**（[浙大温室导航改进 FAST-LIO2，Comput. Electron. Agric. 2025](https://www.booksci.cn/literature/143645109.htm)）：在世界系初始化地平面 z=0，把当前帧地面残差加入 IESKF 优化，z 漂移 2.08m → 0.22m。我们试过的 z-prior 是它的简化版
2. **退化检测 + 自适应处理**（[Wiley Advanced Intelligent Systems 2025](https://onlinelibrary.wiley.com/doi/pdfdirect/10.1002/aisy.202500722)）：用 Rayleigh-Ritz 定理求最弱可观测轴，退化因子低于阈值时标记退化、自适应调整降采样
3. **STD 回环**（同上浙大论文）：稳定三角形描述子回环，进一步压低 z 漂移
4. **ROS2 移植已知问题**（[hku-mars/FAST_LIO #234](https://github.com/hku-mars/FAST_LIO/issues/234)）：时间同步只估计固定偏移、Gazebo 频率过低——与本仿真环境的既有结论一致

---

## 五、12 小时应急方案（推荐执行路径）

### 第 1 步（立即）：用当前配置跑一场完整图

- 终端 4 用 `python3 lio_map_builder.py --guard 0`（其余终端按启动手册）
- 驾驶纪律：**每条走廊只走一遍不折返**、直行 0.3~0.5 m/s、开阔处慢转、角落不停留
- 目的：关键帧全程录制 + 无双线 + z 由 merge 对齐兜底

### 第 2 步：save_run + 验收 merged

```bash
bash ~/matrix_robotac_workspace/matrix_robotac_first/user_code/save_run.sh
~/cloudcompare_install/bin/CloudCompare ~/robotac_maps/run_<最新时间戳>/merged.pcd
```
验收：覆盖全程 ✅、高度统一 ✅、无双线（不折返就没有）✅、散点少 ✅。

### 第 3 步（时间允许才做）：改良版地面约束

若第 1 步的 merged 仍有可见 z 问题，实施"**仅在退化时启用**的 z 约束"：
在 updateLossFunc 里计算 H 的最小特征值，低于阈值（退化）时才把 z-prior 加入
——避开"z-prior 全天候耦合 x/y"的问题，只在退化场景提供 z 锚定。
（改动 ~20 行 C++，编译 30s，风险可控，且有回退路径。）

### 不做的事（12 小时内）

- ❌ 不再动 PGO 回环（从未成功，风险高）
- ❌ 不再调 LIO 参数（14:44 是实测最优基线）
- ❌ 不再改动 builder 累积策略

---

## 六、验收清单（提交前逐项打勾）

- [ ] merged.pcd 覆盖了驾驶过的全部区域（无"一半"）
- [ ] 高度统一（CloudCompare 俯视/侧视无飞点）
- [ ] 驾驶路线无折返 → 无双线重影
- [ ] 地面点密度正常（地面带可见成片地面）
- [ ] 视频一镜到底 ≤15min ≤500MB
- [ ] PCD 为管线原生输出（未手工修改）
